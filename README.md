# mysite

This repo is split into separate frontend and backend areas.

## Frontend

The Next.js app lives in `frontend/`.

```bash
npm run dev
npm run build
npm run lint
```

Those root commands delegate to `frontend/`. You can also work directly from the frontend folder:

```bash
cd frontend
npm run dev
```

## Backend

Backend-specific code and documentation live in `backend/`.
