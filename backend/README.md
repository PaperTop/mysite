# Backend

Backend-specific code and documentation can live here.

The current app uses Next.js route handlers under `app/api`. Add a separate backend service here only when it needs its own runtime, dependencies, or deployment path.
